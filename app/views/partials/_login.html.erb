<%= form_for :user, url: sessions_path do |user| %>
  <p>
    <%= user.label :email %> <%= user.email_field :email %>
    <%= user.label :enter_password %> <%= user.password_field :password %>
    <%= user.submit value="LOG IN" %>
  </p>

<% end %>